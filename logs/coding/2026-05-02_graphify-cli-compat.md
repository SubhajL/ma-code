# Coding Log — Graphify CLI Compatibility Fix

## Work Summary (2026-05-02) - discovery and RED test update
- Goal: fix the merged Graphify adapter to work with the installed `graphifyy 0.6.4` CLI, which exposes `graphify update <path>` rather than the fake-tested `graphify scan <path> --output ...` shape.
- Discovery path: loaded repo instructions and g-coding workflow; Auggie discovery timed out and recommended local fallback; local inspection covered `.pi/agent/extensions/graphify-adapter.ts`, unit/integration tests, Graphify adapter docs, and installed `graphify --help` output.
- Safety cleanup: removed the accidental untracked repo-root `graphify-out/` generated by probing `graphify update --help` earlier, after user approval.
- TDD next step: update fake-binary tests to expect `update <managed sanitized snapshot>` and fail before implementation because the adapter still invokes `scan` with output flags.

## Work Summary (2026-05-02) - RED/GREEN implementation
- RED command: isolated temp runtime with `node --import tsx --test tests/extension-units/graphify-adapter.test.ts tests/integration/graphify-adapter.test.ts`.
- RED result: failed because tests expected real CLI shape `update <managed-source-snapshot>` while the adapter still invoked placeholder `scan` / direct `graph.json` output behavior.
- Files changed:
  - `.pi/agent/extensions/graphify-adapter.ts` — switched adapter invocation to `graphify update <sanitized-source-snapshot>` with `cwd` set to the managed artifact directory; added sanitized source snapshot copying; query now accepts `graph.json`, `graphify-out/graph.json`, or `source-snapshot/graphify-out/graph.json`.
  - `tests/extension-units/graphify-adapter.test.ts` — updated fake binary expectations and added nested real-CLI graph query coverage.
  - `tests/integration/graphify-adapter.test.ts` — verifies the fake binary receives `update <source-snapshot>`, runs from the managed artifact directory, and secret-like local env files are not copied into the snapshot.
  - `.pi/agent/docs/graphify_adapter.md` — documents installed CLI shape and `source-snapshot/graphify-out/graph.json` location.
  - `logs/CURRENT.md`, `reports/planning/2026-05-02_graphify-cli-compat-plan.md`, and this coding log.
- GREEN / validation:
  - `bash scripts/check-foundation-extension-compile.sh` -> `foundation-extension-compile-ok`.
  - `bash scripts/validate-extension-unit-tests.sh` -> PASS, 3 consecutive runs.
  - `bash scripts/validate-core-workflows.sh` -> PASS, 3 consecutive runs.
  - `bash scripts/check-repo-static.sh` -> `repo-static-checks-ok`.
  - `git diff --check` -> no output.
- Real installed CLI smoke:
  - command shape: `cd <managed-temp-artifact> && graphify update <managed-temp-artifact>/source-snapshot`.
  - result: `real-graphify-update-smoke-ok`; Graphify wrote `source-snapshot/graphify-out/graph.json`, confirming the adapter should look there for real CLI output.
- Wiring verification:
  - Existing `.pi/settings.json` extension autoload still covers `.pi/agent/extensions/graphify-adapter.ts`.
  - Existing compile/unit/core validators already include Graphify adapter from PR #50 and passed after the compatibility fix.
- Risk notes:
  - Graphify's output path is tied to the input path for `update`; the adapter preserves managed output by scanning a managed sanitized snapshot, not the raw repo path.
  - The managed snapshot may contain non-sensitive source copies and remains under ignored `.pi/agent/artifacts/graphify/<task-id>/`.

## Validation Refresh (2026-05-02) - final flake pass
- After adjusting the nested query unit fixture to use `source-snapshot/graphify-out/graph.json`, reran the changed test scope again.
- Final validation:
  - `bash scripts/check-foundation-extension-compile.sh` -> `foundation-extension-compile-ok`.
  - `bash scripts/validate-extension-unit-tests.sh` -> PASS, 3 consecutive final runs.
  - `bash scripts/validate-core-workflows.sh` -> PASS, 3 consecutive final runs.
  - `bash scripts/check-repo-static.sh` -> `repo-static-checks-ok`.
  - `git diff --check` -> no output.
- Generated validation reports were cleaned from the worktree after evidence capture; they are not part of the intended commit.

## Review (2026-05-02) - working-tree Graphify CLI compatibility fix

### Reviewed
- Repo: `/Users/subhajlimanond/dev/ma-code-worktrees/graphify-cli-compat`
- Branch: `split/graphify-cli-compat`
- Scope: working-tree Graphify adapter compatibility fix
- Commands Run:
  - `git diff --stat`
  - `git diff --name-only`
  - `git diff -- .pi/agent/extensions/graphify-adapter.ts`
  - `git diff -- tests/extension-units/graphify-adapter.test.ts tests/integration/graphify-adapter.test.ts .pi/agent/docs/graphify_adapter.md`
  - `bash scripts/check-foundation-extension-compile.sh`
  - `bash scripts/validate-extension-unit-tests.sh` (3 final consecutive runs)
  - `bash scripts/validate-core-workflows.sh` (3 final consecutive runs)
  - `bash scripts/check-repo-static.sh`
  - `git diff --check`

### Findings
CRITICAL
- none

HIGH
- none

MEDIUM
- none

LOW
- none

### Open Questions / Assumptions
- Assumption: `graphify update <path>` remains the stable no-LLM structural update command for installed `graphifyy 0.6.4`.
- Assumption: scanning a managed sanitized snapshot is acceptable because Graphify writes `graphify-out/` relative to its input path, not the process working directory.

### Recommended Tests / Validation
- Passed: `bash scripts/check-foundation-extension-compile.sh`
- Passed: `bash scripts/validate-extension-unit-tests.sh` (3 consecutive final runs)
- Passed: `bash scripts/validate-core-workflows.sh` (3 consecutive final runs)
- Passed: `bash scripts/check-repo-static.sh`
- Passed: `git diff --check`
- Passed: real installed CLI smoke using `graphify update <managed-temp-artifact>/source-snapshot`, producing `source-snapshot/graphify-out/graph.json`.

### Rollout Notes
- Generated Graphify outputs remain ignored under `.pi/agent/artifacts/graphify/<task-id>/`.
- Do not enable deep/semantic/multimodal/URL modes without a separate explicit approval field and tests.

Review Verdict: no_required_fixes
