# Weekly Summary — 2026-05-19 → 2026-05-26

Handoff digest so the next session can pick up the harness-cleanup initiative
without re-reading every PR. Authoritative status lives in
`docs/initiatives/harness-cleanup/` (architectural-review.md, tier-1-status.md,
coverage-audit.md); this is the cross-day narrative.

## Activity window

- **2026-05-19 → 2026-05-22:** no merges on `main`. Only stash/workspace
  commits on a `workspace/root-clean-task-…` branch (worktree housekeeping,
  not initiative work).
- **2026-05-23:** Tier 0 foundation push — 7 PRs.
- **2026-05-24:** Tier 1 & Tier 2 main push — 17 PRs (full sweep of the
  "do-soon" cleanup items plus three Tier 2 closures).
- **2026-05-25 → 2026-05-26 (today):** no merges. Nothing in-flight; open-PR
  list is empty.

Total: ~24 merged PRs (#178–#202), all on the harness-cleanup initiative.

## What landed by tier

### Tier 0 — invariants and visibility (all done)

| PR | Title | Note |
|---|---|---|
| #178 | docs(safety): clarify safe-bash is a guardrail, not a security boundary | AGENTS.md §Safety now states the regex tripwire is **not** a sandbox. |
| #180 | feat(harness): add pi-doctor health check command | Read-only check over runtime state, schemas, models config, audit log. |
| #181 | docs(audit): tier 0 harness coverage and reachability audit | coverage-audit.md. |
| #182 | feat(telemetry): tested cache-telemetry utility + gap doc | `summarizeUsage` / `aggregateCacheTelemetry`; deliberately unwired pending upstream per-call hook. |
| #183 | chore(typecheck): add root tsconfig and npm typecheck script | Exposed a **45-error baseline** across 14 files. |
| #188 | ci(typecheck): wire root typecheck into CI with baseline ratchet | `.typecheck-baseline-count` (currently `45`) prevents drift up; burndown allowed. |

Tier 0 is closed; remaining items are gated on burndown of the 45-error
baseline (Tier 0 follow-up #163 in architectural-review.md).

### Tier 1 — do-soon (all four items moved)

Item 1 — **SQLite runtime state** (done).

5-PR stack, foundation → per-table flip → audit log:

- #192 SQLite runtime-state foundation (`.pi/agent/state/runtime/pi.db`,
  tables `tasks`, `queue_jobs`, `leases`, `audit_log`; unique index on
  `leases(scope)` for atomic CAS).
- #195 `execution-leases` → SQLite source of truth.
- #196 `tasks-state` → SQLite source of truth.
- #197 `queue-state` → SQLite source of truth.
- #198 audit-log dual-write through lib (JSONL retained for ops debugging,
  SQLite is the queryable source).

JSON files auto-migrate/archive on first SQLite boot. JSON schemas stay as
documentation.

Item 2 — **Invert tool surface** (done).

- #189 scope-check audit: `safe-bash.ts` is a local interceptor over the
  upstream `bash` tool; the typed-tool inversion path is feasible.
- #190 typed `git_commit` + `run_test` tools (Tier 1 §2 PR 1).
- #191 `safe-bash` redirects `git commit` and `npm test` bash invocations to
  typed tools (PR 2).
- #199 closes Tier 1 §2: rest of the typed git surface (`git_branch`,
  `git_checkout`, `git_push`); `safe-bash` now blocks the matching bash
  forms with typed-tool guidance.

Residual bash still allowed for one-off shell utilities and still gated by
regex tripwire + protected-path checks + audit log. **Not** a sandbox by
design.

Item 3 — **Prompt caching** (done; see #182 above).

Producer side already in upstream `@mariozechner/pi-ai`. Consumer-side
utility tested and ready; wiring is two lines once `pi-coding-agent` exposes
a per-call hook.

Item 4 — **In-process dispatch** (done).

- #185 central dispatch table (`scripts/lib/harness-dispatch.ts`) + 3 of 12
  operator subcommands in-process (status, leases, queue-session).
- #187 quick wins: bump `claude-opus-4-5` → `claude-opus-4-7` in
  `models.json` + 3 more in-process subcommands.
- #193 3 more in-process subcommands (issue-materialize, afk-orchestrate,
  pr-lifecycle).
- #194 closes Tier 1 §4: **all 12 operator subcommands now in-process**.

Heavy spawn-based subcommands (e.g. `orchestrator-run.ts`) still spawn by
design — the dispatch table is additive and each subcommand keeps its own
isolation tradeoff.

Status trackers updated alongside: #184, #186.

### Tier 2 — do-this-quarter (3 of 4 done)

| PR | Item | Status after PR |
|---|---|---|
| #200 | Collapse orchestrator command dispatch | §5 (orchestrator FSM collapse) progressed. |
| #201 | Consolidate small extension modules | §6 done — `recovery.ts`, `packets.ts`, `stitch.ts` consolidated; old import paths kept for compatibility. |
| #202 | Codegen TS types from JSON schemas | §7 done — `scripts/codegen-schema-types.ts`, `schemas/greenfield/user.types.generated.ts`, `npm run codegen:schema-types` / `check:schema-types`. |

§8 (provider/capability abstraction) is the remaining Tier 2 item — only
partially mooted by the Opus 4.5→4.7 bump in #187; the abstraction itself is
still open.

### Tier 3 — when convenient

All four items still open (phase-number rename, validator runner unification,
`harness-actions.jsonl` rotation, ~~pi-session html move~~ dropped as a
misread in #186).

## Repo state snapshot (2026-05-26 morning)

- `main` is clean (no untracked, no in-flight branch).
- No open PRs.
- Typecheck baseline: **45** errors across 14 files (`.typecheck-baseline-count`).
- Per-task coding logs from 2026-05-24 cover the four biggest landings:
  - `2026-05-24-11-51-20 Coding Log (tier-1-typed-git-tools).md`
  - `2026-05-24-14-11-01 Coding Log (tier2-collapse-orchestrator-fsm).md`
  - `2026-05-24-16-04-19 Coding Log (tier2-consolidate-small-modules).md`
  - `2026-05-24-20-06-17 Coding Log (tier2-codegen-ts-types).md`
- `.codex/coding-log.current` still points at the codegen log — bump it if a
  new task starts.

## Suggested pickups (in priority order)

1. **Typecheck baseline burndown** (Tier 0 follow-up). 45 errors across 14
   files is the only Tier 0 item not closed; ratchet exists so burndown is
   safe and incremental. Cheapest visible win.
2. **Tier 2 §8 — provider/capability abstraction.** `models.json` is bumped
   but `pi-ai`/harness still hard-codes provider concepts in places. This is
   the last Tier 2 item.
3. **Tier 3 items.** Pick whichever the team has appetite for; rotation on
   `harness-actions.jsonl` is the most operationally useful, validator-runner
   unification is the largest cleanup.
4. **Retire `check-foundation-extension-compile.sh`** once baseline reaches
   0 (gated on item 1).

## Pointers for next session

- Authoritative status:
  `docs/initiatives/harness-cleanup/architectural-review.md` (refreshed
  2026-05-24 against current repo).
- Tier 1 detail: `docs/initiatives/harness-cleanup/tier-1-status.md`.
- Tier 0 coverage map: `docs/initiatives/harness-cleanup/coverage-audit.md`.
- Per-task logs: `coding-logs/2026-05-24-*.md`.
- Memory: `MEMORY.md` → `reference_cleanup_initiative_docs.md` (already
  records that the tier docs are the source of truth).

## Caveats

- This is a digest, not a plan. Each pickup item gets its own design pass
  before implementation lands (consistent with the tier-1-status convention).
- Per-task logs from 2026-05-23 do not exist in `coding-logs/`; reconstruct
  from PRs #178–#184 if depth is needed.
