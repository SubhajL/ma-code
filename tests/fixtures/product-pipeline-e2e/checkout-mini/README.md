# checkout-mini Product Pipeline E2E Fixture

`checkout-mini` is a fake UI-facing initiative used by the Phase 14 product pipeline E2E pilot.

The pilot validates the harness pipeline from product intake through quality readiness using only temp repos and fake boundaries. It must not call live Stitch/providers, create runtime task JSON, or generate product implementation code outside the temp fixture workspace.
